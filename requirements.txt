# Core Django framework
Django==5.2.7

# Environment variable management
python-dotenv==1.0.1

# Word document generation
python-docx==1.2.0
lxml==6.0.2

# Image processing
Pillow==12.0.0

# Excel file handling
openpyxl==3.1.5
pandas>=1.5.0
xlrd>=2.0.0
xlsxwriter>=3.0.0
dbfread>=2.0.7  # For reading DBF files (dBase, FoxPro)
django-import-export>=3.3.0  # Import/Export data from Excel, CSV

# Production WSGI server (for deployment)
# Using waitress for cross-platform compatibility (Windows/Linux)
waitress==3.0.1

# Static files serving in production
whitenoise==6.8.2

# Date/time utilities
python-dateutil==2.9.0.post0

# Word to HTML conversion for preview
mammoth==1.8.0

# HTTP library for testing
requests==2.32.3

# Vietnamese text normalization (for quiz question search)
Unidecode>=1.3.6

# OCR Tool
pytesseract>=0.3.13  # Python wrapper cho Tesseract OCR engine
pdf2image>=1.17.0    # Chuyển PDF scan thành ảnh (yêu cầu Poppler cài trên máy chủ)
pdfplumber>=0.11.0   # Trích xuất text trực tiếp từ PDF có text selectable (ưu tiên dùng)

# PDF Tools (Tách / Ghép PDF)
pypdf>=4.0.0         # Đọc, tách, ghép file PDF thuần Python
